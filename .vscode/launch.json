{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Playwright: current test (headed)",
      "program": "${workspaceFolder}/node_modules/playwright/cli.js",
      "args": ["test", "--config", "qa/e2e/playwright.config.ts", "--headed", "${file}:${lineNumber}"],
      "cwd": "${workspaceFolder}"
    }
  ]
}